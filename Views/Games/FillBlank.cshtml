@model ProjectSpetses.Models.Entities.Blank_item

<h1>Fill the blanks!</h1>
<br>
<form asp-action="@nameof(GamesController.SubmitFillBlank)" method="post">

    <div>
        <h4>@Model.Description1  
            <input type="hidden" name="Answers[0].QuestionId" value="@Model.Id" />
            <input type="hidden" name="Answers[0].GameType" value="FillBlank" />
            <select name="Answers[0].SelectedValue" required>
                <option value="" disabled selected>Επίλεξε την Απάντηση</option>
                @foreach (var answer in Model.Answers)
                {
                    <option 
                        value="@(Model.Solution == answer ? "True" : "False")">
                        @answer
                    </option>
                }
            </select>
        @Model.Description2</h4>
    </div>

    <button type="submit">Submit</button>
</form>

@* @model List<ProjectSpetses.Models.Entities.Blank_item>

@foreach (var quiz in Model)
{
    <div>
        <h3>@quiz.Description1 ____ @quiz.Description2</h3>
        <ul>
            @foreach (var answer in quiz.Answers)
            {
                <li>
                    <input type="radio"
                           id="@quiz.Id@answer"
                           name="@quiz.Id"
                           value="@(quiz.Solution == answer ? "True" : "False")"
                           required />
                    <!-- The Value is visible if you inspect the browser page, this is a feature -->
                    <label for="@quiz.Id@answer">@answer</label>
                </li>
            }
        </ul>
    </div>
} *@